var $ = require('jquery');
var Backbone = require('backbone');
var _ = require('underscore');

Backbone.$ = $;


var ProjectsListCollectionClass = require('./node_modules/projectsList/ProjectsListCollection');
var projectsListCollection = new ProjectsListCollectionClass();

module.exports = Backbone.View.extend({

  initialize: function () {
    var _this = this;

    this.render();
  },

  render: function () {
    projectsListCollection.fetch({
      success: function (datas) {
      	console.log(datas);
        //projectView = new ProjectViewClass({ el: $('#portfolio figure'), model: datas.get(id) });
      }
    });
  }
});