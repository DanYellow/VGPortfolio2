var $ = require('jquery');
var Backbone = require('backbone');
var _ = require('underscore');

Backbone.$ = $;

var ProjectsListItemTpl = require('../tpl/ProjectsListItemTpl.hbs');

var ProjectsListCollectionClass = require('../projectsList/ProjectsListCollection');
var projectsListCollection = new ProjectsListCollectionClass();

module.exports = Backbone.View.extend({
  events: {
    "click" : "myClickHandler"
  },

  initialize: function () {
    var _this = this;

    this.render();
  },

  render: function () {
    this.$el.append(ProjectsListItemTpl(this.model.attributes));
    return this;
  },

  myClickHandler: function(e) {
    e.preventDefault();
    console.log($(e.currentTarget));
  }
});