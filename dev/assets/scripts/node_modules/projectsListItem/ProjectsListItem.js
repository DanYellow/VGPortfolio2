var $ = require('jquery');
var Backbone = require('backbone');
var _ = require('underscore');

Backbone.$ = $;

var ProjectsListItemTpl = require('tpl/ProjectListItemTpl');


module.exports = Backbone.View.extend({
  tagName: "li",

  events: {
    "click" : "myClickHandler"
  },

  initialize: function () {
    var _this = this;
  },

  render: function () {
    console.log(ProjectsListItemTpl());
    // this.$el.append(ProjectsListItemTpl(this.model.toJSON()));
    return this;
  }
});